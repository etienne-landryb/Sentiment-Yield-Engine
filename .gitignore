# Secrets
.env
.streamlit/secrets.toml
gca.json
*service-account*.json
*sa-key*.json
credentials*.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.ipynb_checkpoints/

# Cached / generated data
data/raw/*
data/processed/*
data/cache/*
!data/raw/.gitkeep
!data/processed/.gitkeep
!data/cache/.gitkeep

# Date-aware-themes staged rollout: local test artifacts only, never the real
# themes.parquet — guards the Stage 0/1 isolation constraint at the tooling
# level, not just by discipline.
data/snapshot/*_test.parquet

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/
